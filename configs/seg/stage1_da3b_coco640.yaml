trainer:
  logger:
    class_path: lightning.pytorch.loggers.WandbLogger
    init_args:
      project: da3_seg_coco
      name: stage1_da3b_coco640
  devices: 8
  accelerator: gpu
  strategy: ddp_find_unused_parameters_true
  precision: 16-mixed
  max_epochs: 20
  # 🔧 添加callbacks配置
  callbacks:
    - class_path: lightning.pytorch.callbacks.ModelCheckpoint
      init_args:
        dirpath: checkpoints/stage1_da3b_coco640  # 保存目录
        filename: 'epoch={epoch}-step={step}-loss={train_loss:.4f}'  # 文件名格式
        monitor: train_loss  # 监控的指标
        mode: min  # 最小化loss
        save_top_k: 1  # 保存最好的3个
        save_last: true  # 同时保存last.ckpt
        save_on_train_epoch_end: false  # 每个epoch结束保存
        verbose: true
    - class_path: lightning.pytorch.callbacks.LearningRateMonitor
      init_args:
        logging_interval: step
    - class_path: lightning.pytorch.callbacks.ModelSummary
      init_args:
        max_depth: 3

model:
  da3_pretrained_path: "/cache/model/da3/"
  network_config:
    net:
      name: vitb
      out_layers: [5, 7, 9, 11]
      alt_start: 4
      qknorm_start: 4
      rope_start: 4
      cat_token: true
      seg_cfg:
        enable: true
        num_bottleneck_tokens: 16
        num_queries: 100
        num_masked_layers: 4
        layer_start: 6
        dropout: 0.0
        attn_dropout: 0.0
    head:
      dim_in: 1536
      output_dim: 2
      features: 128
      out_channels: [96, 192, 384, 768]
  img_size: [518, 518]
  num_classes: 133
  stuff_classes: []
  attn_mask_annealing_enabled: true
  num_masked_layers: 4
  lr:          0.0004      # 从 2e-4 提到 4e-4
  llrd:        0.8         # 保持不变
  weight_decay: 0.04       # 保持不变
  warmup_steps: [1000, 2000]
  poly_power:  0.9         # 保持不变

data:
  root: /home/jovyan/ybai_ws/dataset/dataset/coco
  panoptic_json_train: /home/jovyan/ybai_ws/dataset/dataset/coco/annotations/panoptic_train2017.json
  panoptic_json_val: /home/jovyan/ybai_ws/dataset/dataset/coco/annotations/panoptic_val2017.json
  stuff_classes: []
  batch_size_per_gpu: 10
  num_workers: 8
  img_size: [518, 518]